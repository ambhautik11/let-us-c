#include<stdio.h>
void main( )
{
    int a[10] ,i,j,k,temp ;
   for (i=0 ; i<10 ; i++)
     scanf("%d",&a[i]);
   
   for (i=1 ; i < 10 ; i++);
    {
    	 printf("jdj");
        for (j=0 ; j<i ; j++);
        { 
             if( a[i] < a[j] )
             { 
                temp = a[i] ;
                for (k=i ; k <j ; k-- )
                       a[k] = a[k-1] ;
                a[j] = temp ;
                
             }

        }

    }
   for (i=0 ; i<10 ; i++)
     printf("%d     ",a[i]);


}